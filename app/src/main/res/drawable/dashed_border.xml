<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="@android:color/transparent" /> <!-- Fondo transparente -->
    <stroke
        android:width="2dp"
        android:color="@color/celeste_boton"
        android:dashWidth="8dp"
        android:dashGap="4dp"/> <!-- Configuración del borde discontínuo -->
    <corners android:radius="10dp"/> <!-- Esquinas redondeadas -->
</shape>
